trigger:
  branches:
    exclude:
    - '*'
  tags:
    include:
    - release-*

pr: none

extends:
  template: templates/node-app-service-deploy.yml
  parameters:
    environmentName: 'Production'
    appDirectory: 'client'
    artifactName: 'turnkeyops-web'
    azureSubscription: '$(AZURE_SERVICE_CONNECTION)'
    resourceGroupName: '$(TURNKEYOPS_PRODUCTION_RESOURCE_GROUP)'
    webAppName: '$(TURNKEYOPS_WEB_APP_NAME)'
    apiBaseUrl: '$(TURNKEYOPS_API_BASE_URL)'
    deployBranch: '__tag_only__'
    deployTagPrefix: 'release-'
    requireManualApproval: true
    smokeApiBaseUrl: '$(TURNKEYOPS_API_BASE_URL)'
    smokeWebBaseUrl: '$(TURNKEYOPS_WEB_BASE_URL)'
