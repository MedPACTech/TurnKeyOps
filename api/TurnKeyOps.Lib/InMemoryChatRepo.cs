using System;

namespace MedInsights.Models
{
   // public class InMemoryChatRepo : IChatRepo
 //   {
        // private readonly Dictionary<string, Chat> _store = new();
        // public Task<Chat?> GetAsync(string chatId, CancellationToken ct) =>
        //     Task.FromResult(_store.TryGetValue(chatId, out var c) ? c : null);

        // public Task UpsertAsync(Chat chat, CancellationToken ct)
        // {
        //     _store[chat.Id] = chat with { UpdatedAt = DateTime.UtcNow };
        //     return Task.CompletedTask;
        // }
//    }
}